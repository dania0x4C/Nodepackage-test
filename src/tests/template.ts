/**
 * 패키지 테스트 템플릿 파일
 * 
 * 사용 방법:
 * 1. 이 파일을 복사하여 [패키지명]-test.ts로 저장합니다.
 * 2. 필요한 패키지를 설치합니다 (npm install [패키지명])
 * 3. 아래 코드를 수정하여 테스트를 진행합니다.
 * 4. 다음 명령어로 실행합니다: npm run run:test src/tests/[패키지명]-test.ts
 */

// 패키지 import
// import packageName from 'package-name';

/**
 * 테스트 코드 작성
 */
function runTest() {
  console.log('테스트 시작...');
  
  // 여기에 테스트 코드를 작성합니다
  
  console.log('테스트 완료!');
}

// 테스트 실행
runTest(); 